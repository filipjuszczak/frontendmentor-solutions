import styled from "styled-components";

export const StyledCard = styled.article`
  max-width: min(90%, 20rem);
  padding: 1.5rem;
  border-radius: 15px;
  background-color: hsl(var(--bg-clr-card));
`;
